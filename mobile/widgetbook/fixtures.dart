/// Fixture data shared across Widgetbook use-cases.
///
/// Re-exported from `test/goldens/fixtures.dart`, which owns the data so the
/// catalogue and the golden tests never drift apart (DS4b).
library;

export '../test/goldens/fixtures.dart';
