# Test environment configuration
# Database: provided by Testcontainers (PostGIS) via TestContainersConfig + @ServiceConnection.
# Requires a running Docker daemon. No local docker-compose database is needed for tests.
spring:
