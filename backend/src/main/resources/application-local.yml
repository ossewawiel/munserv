# Local development overrides
spring:
  jpa:
    show-sql: true
  mail:
    host: smtp.gmail.com          # Change to your SMTP provider
    port: 587
    username: marsel.pretorius@gmail.com  # Your email
    password: uunm xebm cskx aceq     # Your app password (NOT your regular password)

# Redirect all emails to dev address (prevents sending to real users during testing)
munserv:
  email:
    override-recipient: ${EMAIL_OVERRIDE_RECIPIENT:marsel.pretorius@gmail.com}

# Bootstrap super user for local development
# WARNING: Development only - never use these in production
bootstrap:
  super-user:
    enabled: true
    email: superuser@munserv.local
    password: super123

logging:
  level:
    com.munserv: DEBUG
